import Link from "next/link"; import type {Metadata} from "next"; import {articles} from "@/content/writing"; import styles from "./writing.module.css";
export const metadata:Metadata={title:"Writing | Rasik Tiwari",description:"Practical notes on BI, data quality, troubleshooting and learning through doing.",alternates:{canonical:"/writing"}};
export default function WritingPage(){return <main className={styles.page}><div className={styles.inner}><Link href="/" className={styles.backLink}>← Back to portfolio</Link><header className={styles.header}><p className={styles.eyebrow}>Writing</p><h1>Notes from the workbench.</h1><p>Practical writing about BI, data quality, troubleshooting and learning through doing.</p></header><div className={styles.grid}>{articles.map(article=><article key={article.slug} className={styles.card}><p className={styles.meta}>{article.published} · {article.readingTime}</p><h2><Link href={"/writing/"+article.slug}>{article.title}</Link></h2><p>{article.description}</p><div className={styles.tags}>{article.tags.map(tag=><span key={tag}>{tag}</span>)}</div></article>)}</div></div></main>}